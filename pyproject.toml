[project]
name = "chloe"
version = "0.1.0"       #TODO: Set the version
description = ""        #TODO: Set a description
readme = "README.md"
authors = [
    { name = "test.", email = "test@test.com" } #TODO: Set the authors.
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
